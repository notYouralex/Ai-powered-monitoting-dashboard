apiVersion: 1

# Host-installed Grafana uses a manually configured Monitoring API datasource.
# Keep repository provisioning inert so local datasource UIDs and credentials
# remain in Grafana's secure configuration instead of source control.
datasources: []
