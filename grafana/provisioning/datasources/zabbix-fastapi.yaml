apiVersion: 1

# Legacy Zabbix-specific datasource provisioning is intentionally inert.
# The Zabbix dashboard uses the same manually configured Monitoring API
# Infinity datasource as the other project dashboards.
datasources: []
