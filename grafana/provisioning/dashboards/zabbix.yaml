apiVersion: 1

# Legacy Zabbix-specific file provisioning is intentionally inert.
# Import zabbix-infrastructure.json through the existing Grafana UI and select
# the securely configured Monitoring API datasource.
providers: []
